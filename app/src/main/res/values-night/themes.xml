<resources>
    <!-- Base application theme. -->
    <style name="Theme.Test" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">#002D62</item> <!-- Navy blue -->
        <item name="colorPrimaryVariant">#002D62</item> <!-- Navy blue -->
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">#C8102E</item> <!-- Red -->
        <item name="colorSecondaryVariant">#9B1B30</item> <!-- Dark red -->
        <item name="colorOnSecondary">@color/white</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">@color/black</item>
        <!-- Customize your theme here. -->
        <item name="android:windowBackground">@android:color/white</item> <!-- White background -->
        <item name="android:textColorPrimary">@android:color/white</item> <!-- White text in top and bottom bars -->
        <item name="android:textColorSecondary">@android:color/white</item>
        <item name="android:textColor">@android:color/black</item> <!-- Black screen text -->
    </style>


</resources>